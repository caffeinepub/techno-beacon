{
  "frontend/src/index.css": "Global dark industrial techno theme with OKLCH color tokens, Space Grotesk/Space Mono fonts, neon amber/green accent utilities, scanline overlay, and card hover/glow animations.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Space Grotesk/Mono fonts, neon amber/green color tokens, sharp border radii, neon box-shadow utilities, and custom keyframe animations for glow and pulse effects.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks including three new radar-event hooks — `useRadarEvents` (fetches saved events for authenticated user), `useAddEventToRadar` (mutation to save an event, invalidates radarEvents cache), and `useRemoveEventFromRadar` (mutation to unsave an event, invalidates radarEvents cache).",
  "frontend/src/components/SourceBadge.tsx": "Small badge component displaying event source labels (RA, Bandsintown, Festicket) with color-coded neon accent styling.",
  "frontend/src/components/EventCard.tsx": "Event card component with Plan Trip, Tickets, and a new 📡 Add to Radar / On Radar toggle button; unauthenticated users see a toast prompt, authenticated users can save/unsave events with toast feedback and active amber styling when saved.",
  "frontend/src/components/EventFilters.tsx": "Filter bar with artist name, city, and date range inputs with active filter chips; uses `shrink-0` (Tailwind v3) instead of `flex-shrink-0`.",
  "frontend/src/components/ArtistCard.tsx": "Artist card component showing image, name, genre, event count, and follow button — unauthenticated users see all card data; clicking follow without login shows a toast notification instead of triggering a full-page login prompt.",
  "frontend/src/components/LoginPrompt.tsx": "Centered login prompt component with neon icon, title, message, and a Login button that triggers Internet Identity authentication.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time login profile setup, prompting the user for their name and saving it via the backend; prevents flash by checking actor state.",
  "frontend/src/components/Navigation.tsx": "Sticky top navigation with logo, Discover/Artists/My Radar links, active state highlighting, and auth controls (Login button or user name + Logout); includes responsive mobile menu.",
  "frontend/src/pages/DiscoverPage.tsx": "Discover page with hero banner, artist chips, event filters, and event grid — now includes full error handling for both `useGetArtists` and `useGetAllEvents` failures; displays a prominent \"Unable to connect to the backend\" alert with a retry button when either query errors, logs errors to console, and hides artist chips/event grid when data is unavailable due to errors.",
  "frontend/src/pages/ArtistsPage.tsx": "Artists page with search, artist grid, and improved error handling — when `getArtists` fails (e.g. canister stopped/IC0508), shows a clear \"Unable to connect to the backend\" message with a retry button, logs the full error to console via `useEffect`, and correctly distinguishes error state from empty/loading states.",
  "frontend/src/pages/ArtistDetailPage.tsx": "Artist detail page showing artist info and upcoming events — fully accessible to unauthenticated users; follow/unfollow button shows a toast prompting login when clicked without authentication instead of rendering a LoginPrompt component.",
  "frontend/src/pages/MyRadarPage.tsx": "My Radar page with two distinct sections — a new \"Saved Events\" section sourced from `useRadarEvents` (sorted chronologically, with empty-state prompt and per-event ticket links) and the existing \"Upcoming Events\" section from tracked artists; summary stats now include a \"Saved Events\" count.",
  "frontend/src/pages/NotFoundPage.tsx": "404 not-found page with neon pulsing icon, large mono 404 text, and a link back to the Discover page.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup, Layout component (Navigation + footer + ProfileSetupModal + Toaster), and routes for Discover, Artists, Artist Detail (now using `$artistId` param), and My Radar pages.",
  "frontend/index.html": "HTML entry point with Techno Beacon title and favicon pointing to the generated logo asset.",
  "frontend/src/components/ArtistEventPopup.tsx": "Artist event popup with Plan Trip, Tickets, and a new 📡 Add to Radar / On Radar toggle button on each event row; identical authentication gating and toast feedback as EventCard, with compact sizing to fit the popup layout.",
  "frontend/src/components/AdminInitButton.tsx": "AdminInitButton component that shows a \"No artist data found\" banner with a prominent amber \"Admin only\" badge (using shadcn Badge + ShieldAlert icon), a description, spinner during loading, and success/error toast notifications via sonner on completion.",
  "frontend/src/utils/travelLinks.ts": "Travel link utility with a comprehensive IATA airport code lookup map (80+ cities covering all seeded event cities), a `cityToIata()` resolver that normalises city names and falls back gracefully, and `generateSkyscannerUrl()` that uses IATA codes; duplicate `toronto` key removed; Booking.com helpers unchanged.",
  "frontend/src/components/TripPlannerModal.tsx": "Modal dialog for trip planning — shows event summary, auto-calculated travel dates (day before/after), a home city input persisted to localStorage, a three-option budget selector (Budget/Mid-range/Luxury), and action buttons that open pre-filled Skyscanner flight search and Booking.com hotel search in new tabs."
}