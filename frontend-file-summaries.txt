{
  "frontend/src/index.css": "Global dark industrial techno theme with OKLCH color tokens, Space Grotesk/Space Mono fonts, neon amber/green accent utilities, scanline overlay, and card hover/glow animations.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Space Grotesk/Mono fonts, neon amber/green color tokens, sharp border radii (0px), neon box-shadow utilities, and custom keyframe animations for glow and pulse effects.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the app; `useIsAdmin` now includes `isInitializing` from `useInternetIdentity` in its `enabled` condition and loading state, ensuring the admin query only runs after identity initialization is complete and preventing premature false results.",
  "frontend/src/components/SourceBadge.tsx": "Small badge component displaying event source labels (RA, Bandsintown, Festicket) with color-coded neon accent styling.",
  "frontend/src/components/EventCard.tsx": "Event card with radar toggle, Plan Trip button, and Dice/Songkick ticket links; uses `source` prop (not `label`) for SourceBadge, and conditionally renders TripPlannerModal without an `open` prop.",
  "frontend/src/components/EventFilters.tsx": "Filter bar with artist name, city, and date range inputs with active filter chips; uses shrink-0 and clean Tailwind v3 classes.",
  "frontend/src/components/ArtistCard.tsx": "Artist card component showing image, name, genre, event count, and follow button; passes artistName to ArtistEventPopup and navigates to artist detail page on click.",
  "frontend/src/components/LoginPrompt.tsx": "Centered login prompt component with neon icon, title, message, and a Login button that triggers Internet Identity authentication.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time login profile setup, prompting the user for their name and saving it via the backend; prevents flash by checking actor state.",
  "frontend/src/components/Navigation.tsx": "Sticky top navigation with logo, Discover/Artists/My Radar links, active state highlighting, and auth controls (Login button or user name + Logout); includes responsive mobile menu.",
  "frontend/src/pages/DiscoverPage.tsx": "Discover page with hero banner, artist filter chips, event filters, and event grid; shows a clean empty state with Radio icon and no defensive filtering that could hide valid events.",
  "frontend/src/pages/ArtistsPage.tsx": "Artists page with search, artist grid, and role-aware empty states; `AdminInitButton` is now rendered in its own full-width row below the header (not inline with the search input) so it's always visible and not cramped, with the empty state pointing users to the button above.",
  "frontend/src/pages/ArtistDetailPage.tsx": "Artist detail page showing artist header and event grid with loading/error states; no defensive filtering of events — all events from the backend are displayed as-is.",
  "frontend/src/pages/MyRadarPage.tsx": "My Radar page showing saved events and tracked artists for authenticated users; clean implementation without error banners or defensive filtering.",
  "frontend/src/pages/NotFoundPage.tsx": "404 not-found page with neon pulsing icon, large mono 404 text, and a link back to the Discover page.",
  "frontend/src/App.tsx": "Main app with TanStack Router, QueryClientProvider, ThemeProvider, Layout component that computes showProfileSetup and passes it as the required `open` prop to ProfileSetupModal, and routes for all pages.",
  "frontend/index.html": "HTML entry point with Techno Beacon title and favicon pointing to the generated logo asset.",
  "frontend/src/components/ArtistEventPopup.tsx": "Artist event popup dialog showing per-artist event rows with radar toggle, Plan Trip, and ticket search buttons; conditionally renders TripPlannerModal without an `open` prop by using `{tripEvent && <TripPlannerModal ... />}`.",
  "frontend/src/components/AdminInitButton.tsx": "Admin-only button that checks admin status via `useIsAdmin` and `useActor`; removed the `useRef`-based timeout entirely and replaced it with a simpler loading check that uses `isInitializing` from `useInternetIdentity` to correctly wait for identity/actor readiness before resolving admin status.",
  "frontend/src/utils/travelLinks.ts": "Travel link utility with IATA airport code lookup map for all active event cities; reorganised the \"Other\" section to explicitly group Oslo (OSL) under Norway and Copenhagen (CPH) under Denmark, and added Malta (MLA) and Valletta (MLA) entries to support the new Defected Malta event.",
  "frontend/src/components/TripPlannerModal.tsx": "Modal dialog for trip planning — shows event summary, auto-calculated travel dates (day before/after), a home city input persisted to localStorage, a three-option budget selector (Budget/Mid-range/Luxury), and action buttons that open pre-filled Skyscanner flight search and Booking.com hotel search in new tabs.",
  "frontend/src/lib/eventIdLookup.ts": "Utility mapping (artistId + \"_\" + dateTime nanoseconds) to backend event IDs for all 8 seeded artists covering all events in the seed data.",
  "frontend/src/utils/ticketSearch.ts": "Utility that builds ticket search URLs for Dice.fm (`https://dice.fm/search?q=`) and Songkick (`https://www.songkick.com/search?query=`) using `encodeURIComponent` on the artist name."
}